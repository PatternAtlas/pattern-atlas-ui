class PatternImplementation {
    id: string;
    type: string;
    link: string;

    constructor(id: string, type: string, link: string) {
      this.id = id;
      this.type = type;
      this.link = link;
    }
}

export default PatternImplementation; 
